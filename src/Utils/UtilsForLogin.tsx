import React, { ReactElement } from 'react';

export function LoginContent(): ReactElement {
  return (
    <>
      <text> ololo </text>
      <text> ololo </text>
    </>
  );
}

export function LoginAction(): ReactElement {
  return <button type="button" className="menu-subMenuButtons"> a </button>;
}
